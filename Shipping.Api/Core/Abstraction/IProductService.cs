using Shipping.Api.Core.Domain.Models;
using Shipping.Api.Infrastructure.Dtos;

namespace Shipping.Api.Core.Abstraction
{
    public interface IProductService
    {
        //Task<IEnumerable<ProductReadDTO>> GetAllProductsAsync(int pageNumber, int pageSize, string? search);
        //Task<ProductReadDTO?> GetProductByIdAsync(int id);
        //Task<ProductReadDTO> CreateProductAsync(ProductCreateDTO productDto);
        //Task<bool> UpdateProductAsync(int id, ProductCreateDTO productDto);
        //Task<bool> DeleteProductAsync(int id);
    }
}
